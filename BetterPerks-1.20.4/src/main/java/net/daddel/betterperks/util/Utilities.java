package net.daddel.betterperks.util;


public class Utilities {
    private static StringManager stm = new StringManager();
    public static String version = "v" + stm.getVersion() + "-mc" + stm.getAPIVersion();
}
